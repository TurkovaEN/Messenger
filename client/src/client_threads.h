#pragma once

// Подключаем кроссплатформенное определение типа сокета и сетевых функций
#include "../../common/platform.h"

// Основная функция консольного клиента: логин уже выполнен в вызывающем коде,
// далее запускается поток приёма, обрабатываются команды из stdin и отправка сообщений
int run_client(sock_t s, const char* username);